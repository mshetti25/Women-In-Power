create table constitution (
economy varchar,
code varchar primary key,
region varchar,
income_group varchar,cons
non_discrimination_clause varchar,
non_discrimination_clause_explicit_to_sex varchar,
constitution_equality_clause varchar,
quota_corporate_board varchar,
legislative_quota_for_board varchar
);

select * from constitution;